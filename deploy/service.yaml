apiVersion: v1
kind: Service
metadata:
  name: mcp-openshift-server
  namespace: mcp-openshift-server
  labels:
    app: mcp-openshift-server
spec:
  selector:
    app: mcp-openshift-server
  ports:
    - name: http
      port: 80
      targetPort: http
